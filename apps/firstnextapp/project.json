{
  "name": "firstnextapp",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/firstnextapp",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/firstnextapp",
        "outputPath": "dist/apps/firstnextapp"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/firstnextapp"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "firstnextapp:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "firstnextapp:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "firstnextapp:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "firstnextapp:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/firstnextapp/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/firstnextapp/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
